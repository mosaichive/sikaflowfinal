# Fix the mobile team invitation form

## Scope
- Make the invitation window fit within the phone viewport and allow its content to scroll independently.
- Keep the heading and close control visible while ensuring the module list and submit button are reachable.
- Preserve the existing desktop layout and invitation behavior.

## Validation
- Open the team invitation window at a phone-sized viewport.
- Confirm the full form can scroll from the email field through module access to the submit button without overlap.
- Run the relevant frontend checks.

## Delivery
- The project’s existing GitHub sync will receive the committed project change automatically; no database or backend changes are required.
