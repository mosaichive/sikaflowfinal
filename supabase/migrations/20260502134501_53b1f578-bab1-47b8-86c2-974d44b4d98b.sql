-- Historical super-admin seed intentionally disabled.
-- Platform roles must be assigned through the authenticated, MFA-protected
-- administrative workflow. Migrations must never create Auth users or passwords.
DO $$
BEGIN
  NULL;
END $$;
