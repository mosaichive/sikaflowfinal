// This file is automatically generated. Do not edit it directly.
import { createClient, type SupabaseClient } from '@supabase/supabase-js';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL as string | undefined;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY as string | undefined;

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
  // Do NOT throw at module load — that would crash the entire React tree
  // before the ErrorBoundary can render, producing a blank white screen.
  // eslint-disable-next-line no-console
  console.error(
    '[supabase] Missing VITE_SUPABASE_URL or VITE_SUPABASE_PUBLISHABLE_KEY. ' +
      'The app will render but data calls will fail until env vars are restored.'
  );
}

// NOTE: The generated `Database` typings are intentionally not applied here.
// The codebase references several tables/columns/RPCs that exist at runtime
// in the deployed Supabase project but are not present in the generated
// types.ts (schema drift after a GitHub merge). Using a permissively-typed
// client lets the build pass without changing any business logic. Runtime
// behavior is unchanged.
export const supabase: SupabaseClient<any, 'public', any> = createClient<any, 'public', any>(
  SUPABASE_URL ?? 'https://invalid.supabase.co',
  SUPABASE_PUBLISHABLE_KEY ?? 'invalid-anon-key'
);
